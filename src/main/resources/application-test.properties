# application-test.properties
# This file is for reference only. The actual test properties are in src/test/resources/application-test.properties

# Database Configuration (same as main but with test database)
spring.datasource.url=jdbc:mysql://localhost:3306/sports_training_tracker_test
spring.datasource.username=root
spring.datasource.password=H#Pandita2105
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable runner or startup logic if needed
spring.main.banner-mode=off
